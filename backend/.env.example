# Backend-Konfiguration

# Port auf dem der Server läuft
PORT=3000

# Node-Umgebung (development, test, production)
NODE_ENV=development

# CORS-Ursprung (Frontend-URL)
CORS_ORIGIN=http://localhost:4200

# Datenbank-Konfiguration
# DB_PATH=./data/deeptasker.db (Wird automatisch geladen)
